apiVersion: v1
kind: Service
metadata:
  name: inventario-service
  namespace: diplomado-m3
spec:
  type: ClusterIP
  selector:
    app: inventario
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000