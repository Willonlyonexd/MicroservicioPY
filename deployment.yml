apiVersion: apps/v1
kind: Deployment
metadata:
  name: mss-inventario
  namespace: diplomado-m3
spec:
  selector:
    matchLabels:
      app: inventario
  replicas: 1
  template:
    metadata:
      labels:
        app: inventario
    spec:
      containers:
        - name: inventario
          image: wilsonmontanoclaros/microservicio-python:1.0